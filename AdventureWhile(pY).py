pY = 0
x = []
lM = True

# the adventure of pY

if pY:
    pY = pY + 1  # type: int

Joker = (input('Enter a value: '))
x.append(Joker)

print(x)

x2 = x[:]

print(x2)


# lets run some calculations on pY

# lets get pY into x & x2


# creating a simple ball class
class Ball:
    def __init__(self, color, size, direction):  # initilization
        self.color = color
        self.size = size
        self.direction = direction

    def bounce(self):
        if self.direction == "down":
            self.direction = "up"


Myball = Ball()  # instance



locations =[]
lm = True
metal = False


class Xp:                           # class invocation
    def __sizeof__(self, Px, size): # Object initialized
        self.Px = Px                # methods created #dot notation
        self.size = size            # instance reference #global variables

    def power(self, Px):            # defining a function
        up = self + (self * Px)     # local variable
        return up                   # jump back to program


Px = Xp()
Py = Xp()
print(Py)
print(Px)

# Once pY was just a variable, now Py is now an object [+3 char]

if up > 1:
    print("not enough")
elif:
    print("asas")
elif:
    print("asas")
else:
    print("sds")

