class Triangle:
    def __init__(self, width, height): # 3 parameters initialize  with instance reference
        self.width = width
        self.height = height

    def getArea(self):
        area = self.width * self.height /2
        return area

class Square:
    def __init__(self, size):
        self,size = size

    def getArea(self, size):
        area = self.size * self.size
        return area




    def __sizeof__(self, Px, three):
        self.Px = Px
        self.three = three